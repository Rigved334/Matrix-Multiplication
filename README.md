This program performs multiplication of 2 nxn matrices. Where the user is prompted for:
- rows of first matrix
- column of first matrix
- rows of second matrix
- column of second matrix
- the elements in both matrices
